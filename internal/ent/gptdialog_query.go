// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/go-faster/bot/internal/ent/gptdialog"
	"github.com/go-faster/bot/internal/ent/predicate"
)

// GPTDialogQuery is the builder for querying GPTDialog entities.
type GPTDialogQuery struct {
	config
	ctx        *QueryContext
	order      []gptdialog.Order
	inters     []Interceptor
	predicates []predicate.GPTDialog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GPTDialogQuery builder.
func (gdq *GPTDialogQuery) Where(ps ...predicate.GPTDialog) *GPTDialogQuery {
	gdq.predicates = append(gdq.predicates, ps...)
	return gdq
}

// Limit the number of records to be returned by this query.
func (gdq *GPTDialogQuery) Limit(limit int) *GPTDialogQuery {
	gdq.ctx.Limit = &limit
	return gdq
}

// Offset to start from.
func (gdq *GPTDialogQuery) Offset(offset int) *GPTDialogQuery {
	gdq.ctx.Offset = &offset
	return gdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gdq *GPTDialogQuery) Unique(unique bool) *GPTDialogQuery {
	gdq.ctx.Unique = &unique
	return gdq
}

// Order specifies how the records should be ordered.
func (gdq *GPTDialogQuery) Order(o ...gptdialog.Order) *GPTDialogQuery {
	gdq.order = append(gdq.order, o...)
	return gdq
}

// First returns the first GPTDialog entity from the query.
// Returns a *NotFoundError when no GPTDialog was found.
func (gdq *GPTDialogQuery) First(ctx context.Context) (*GPTDialog, error) {
	nodes, err := gdq.Limit(1).All(setContextOp(ctx, gdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{gptdialog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gdq *GPTDialogQuery) FirstX(ctx context.Context) *GPTDialog {
	node, err := gdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GPTDialog ID from the query.
// Returns a *NotFoundError when no GPTDialog ID was found.
func (gdq *GPTDialogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gdq.Limit(1).IDs(setContextOp(ctx, gdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{gptdialog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gdq *GPTDialogQuery) FirstIDX(ctx context.Context) int {
	id, err := gdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GPTDialog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GPTDialog entity is found.
// Returns a *NotFoundError when no GPTDialog entities are found.
func (gdq *GPTDialogQuery) Only(ctx context.Context) (*GPTDialog, error) {
	nodes, err := gdq.Limit(2).All(setContextOp(ctx, gdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{gptdialog.Label}
	default:
		return nil, &NotSingularError{gptdialog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gdq *GPTDialogQuery) OnlyX(ctx context.Context) *GPTDialog {
	node, err := gdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GPTDialog ID in the query.
// Returns a *NotSingularError when more than one GPTDialog ID is found.
// Returns a *NotFoundError when no entities are found.
func (gdq *GPTDialogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gdq.Limit(2).IDs(setContextOp(ctx, gdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{gptdialog.Label}
	default:
		err = &NotSingularError{gptdialog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gdq *GPTDialogQuery) OnlyIDX(ctx context.Context) int {
	id, err := gdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GPTDialogs.
func (gdq *GPTDialogQuery) All(ctx context.Context) ([]*GPTDialog, error) {
	ctx = setContextOp(ctx, gdq.ctx, "All")
	if err := gdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GPTDialog, *GPTDialogQuery]()
	return withInterceptors[[]*GPTDialog](ctx, gdq, qr, gdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gdq *GPTDialogQuery) AllX(ctx context.Context) []*GPTDialog {
	nodes, err := gdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GPTDialog IDs.
func (gdq *GPTDialogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if gdq.ctx.Unique == nil && gdq.path != nil {
		gdq.Unique(true)
	}
	ctx = setContextOp(ctx, gdq.ctx, "IDs")
	if err = gdq.Select(gptdialog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gdq *GPTDialogQuery) IDsX(ctx context.Context) []int {
	ids, err := gdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gdq *GPTDialogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gdq.ctx, "Count")
	if err := gdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gdq, querierCount[*GPTDialogQuery](), gdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gdq *GPTDialogQuery) CountX(ctx context.Context) int {
	count, err := gdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gdq *GPTDialogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gdq.ctx, "Exist")
	switch _, err := gdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gdq *GPTDialogQuery) ExistX(ctx context.Context) bool {
	exist, err := gdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GPTDialogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gdq *GPTDialogQuery) Clone() *GPTDialogQuery {
	if gdq == nil {
		return nil
	}
	return &GPTDialogQuery{
		config:     gdq.config,
		ctx:        gdq.ctx.Clone(),
		order:      append([]gptdialog.Order{}, gdq.order...),
		inters:     append([]Interceptor{}, gdq.inters...),
		predicates: append([]predicate.GPTDialog{}, gdq.predicates...),
		// clone intermediate query.
		sql:  gdq.sql.Clone(),
		path: gdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PeerID string `json:"peer_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GPTDialog.Query().
//		GroupBy(gptdialog.FieldPeerID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (gdq *GPTDialogQuery) GroupBy(field string, fields ...string) *GPTDialogGroupBy {
	gdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GPTDialogGroupBy{build: gdq}
	grbuild.flds = &gdq.ctx.Fields
	grbuild.label = gptdialog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PeerID string `json:"peer_id,omitempty"`
//	}
//
//	client.GPTDialog.Query().
//		Select(gptdialog.FieldPeerID).
//		Scan(ctx, &v)
func (gdq *GPTDialogQuery) Select(fields ...string) *GPTDialogSelect {
	gdq.ctx.Fields = append(gdq.ctx.Fields, fields...)
	sbuild := &GPTDialogSelect{GPTDialogQuery: gdq}
	sbuild.label = gptdialog.Label
	sbuild.flds, sbuild.scan = &gdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GPTDialogSelect configured with the given aggregations.
func (gdq *GPTDialogQuery) Aggregate(fns ...AggregateFunc) *GPTDialogSelect {
	return gdq.Select().Aggregate(fns...)
}

func (gdq *GPTDialogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gdq); err != nil {
				return err
			}
		}
	}
	for _, f := range gdq.ctx.Fields {
		if !gptdialog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gdq.path != nil {
		prev, err := gdq.path(ctx)
		if err != nil {
			return err
		}
		gdq.sql = prev
	}
	return nil
}

func (gdq *GPTDialogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GPTDialog, error) {
	var (
		nodes = []*GPTDialog{}
		_spec = gdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GPTDialog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GPTDialog{config: gdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gdq *GPTDialogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gdq.querySpec()
	_spec.Node.Columns = gdq.ctx.Fields
	if len(gdq.ctx.Fields) > 0 {
		_spec.Unique = gdq.ctx.Unique != nil && *gdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gdq.driver, _spec)
}

func (gdq *GPTDialogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(gptdialog.Table, gptdialog.Columns, sqlgraph.NewFieldSpec(gptdialog.FieldID, field.TypeInt))
	_spec.From = gdq.sql
	if unique := gdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gdq.path != nil {
		_spec.Unique = true
	}
	if fields := gdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, gptdialog.FieldID)
		for i := range fields {
			if fields[i] != gptdialog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gdq *GPTDialogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gdq.driver.Dialect())
	t1 := builder.Table(gptdialog.Table)
	columns := gdq.ctx.Fields
	if len(columns) == 0 {
		columns = gptdialog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gdq.sql != nil {
		selector = gdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gdq.ctx.Unique != nil && *gdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gdq.predicates {
		p(selector)
	}
	for _, p := range gdq.order {
		p(selector)
	}
	if offset := gdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GPTDialogGroupBy is the group-by builder for GPTDialog entities.
type GPTDialogGroupBy struct {
	selector
	build *GPTDialogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gdgb *GPTDialogGroupBy) Aggregate(fns ...AggregateFunc) *GPTDialogGroupBy {
	gdgb.fns = append(gdgb.fns, fns...)
	return gdgb
}

// Scan applies the selector query and scans the result into the given value.
func (gdgb *GPTDialogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gdgb.build.ctx, "GroupBy")
	if err := gdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GPTDialogQuery, *GPTDialogGroupBy](ctx, gdgb.build, gdgb, gdgb.build.inters, v)
}

func (gdgb *GPTDialogGroupBy) sqlScan(ctx context.Context, root *GPTDialogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gdgb.fns))
	for _, fn := range gdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gdgb.flds)+len(gdgb.fns))
		for _, f := range *gdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GPTDialogSelect is the builder for selecting fields of GPTDialog entities.
type GPTDialogSelect struct {
	*GPTDialogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gds *GPTDialogSelect) Aggregate(fns ...AggregateFunc) *GPTDialogSelect {
	gds.fns = append(gds.fns, fns...)
	return gds
}

// Scan applies the selector query and scans the result into the given value.
func (gds *GPTDialogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gds.ctx, "Select")
	if err := gds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GPTDialogQuery, *GPTDialogSelect](ctx, gds.GPTDialogQuery, gds, gds.inters, v)
}

func (gds *GPTDialogSelect) sqlScan(ctx context.Context, root *GPTDialogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gds.fns))
	for _, fn := range gds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
