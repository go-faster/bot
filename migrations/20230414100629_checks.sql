-- Create "checks" table
CREATE TABLE "checks" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "repo_id" bigint NOT NULL, "name" character varying NOT NULL, "status" character varying NOT NULL, "conclusion" character varying NULL, PRIMARY KEY ("id"));
-- Create index "check_repo_id_id" to table: "checks"
CREATE UNIQUE INDEX "check_repo_id_id" ON "checks" ("repo_id", "id");
